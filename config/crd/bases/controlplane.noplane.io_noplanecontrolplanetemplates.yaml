---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: noplanecontrolplanetemplates.controlplane.noplane.io
spec:
  group: controlplane.noplane.io
  names:
    categories:
    - cluster-api
    kind: NoPlaneControlPlaneTemplate
    listKind: NoPlaneControlPlaneTemplateList
    plural: noplanecontrolplanetemplates
    shortNames:
    - npcpt
    singular: noplanecontrolplanetemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NoPlaneControlPlaneTemplate is the Schema for the noplanecontrolplanetemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NoPlaneControlPlaneTemplate
            properties:
              template:
                description: template defines the control plane template.
                properties:
                  metadata:
                    description: metadata is the metadata applied to the control plane
                      created from this template.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations is an unstructured key value map stored with a resource that may be
                          set by external tools to store and retrieve arbitrary metadata. They are not
                          queryable and should be preserved when modifying objects.
                          More info: http://kubernetes.io/docs/user-guide/annotations
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels is a map of string keys and values that can be used to organize and categorize
                          (scope and select) objects. May match selectors of replication controllers
                          and services.
                          More info: http://kubernetes.io/docs/user-guide/labels
                        type: object
                    type: object
                  spec:
                    description: spec defines the desired state of the control plane.
                    properties:
                      controlPlaneEndpoint:
                        description: |-
                          controlPlaneEndpoint is the endpoint of the provisioned control plane.
                          Populated by the controller once ready. May be set by the user for
                          brownfield adoption.
                        properties:
                          host:
                            description: host is the hostname on which the API server
                              is serving.
                            maxLength: 512
                            type: string
                          port:
                            description: port is the port on which the API server
                              is serving.
                            format: int32
                            type: integer
                        type: object
                      credentialsSecretRef:
                        description: |-
                          credentialsSecretRef references the Secret containing the noplane.io API key.
                          The Secret must contain a key named "apiKey".
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      planeID:
                        description: |-
                          planeID is the noplane.io plane ID.
                          When set by the user, the controller skips creation and adopts the
                          existing control plane. Used for brownfield adoption.
                        type: string
                      replicas:
                        default: 1
                        description: replicas is the desired number of control plane
                          replicas.
                        format: int32
                        type: integer
                      version:
                        description: version is the desired Kubernetes version (e.g.
                          "v1.29.2").
                        maxLength: 256
                        minLength: 1
                        type: string
                    required:
                    - credentialsSecretRef
                    - version
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
