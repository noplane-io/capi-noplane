---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: noplanecontrolplanes.controlplane.cluster.x-k8s.io
spec:
  group: controlplane.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: NoPlaneControlPlane
    listKind: NoPlaneControlPlaneList
    plural: noplanecontrolplanes
    shortNames:
    - npcp
    singular: noplanecontrolplane
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels['cluster\.x-k8s\.io/cluster-name']
      name: Cluster
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.initialized
      name: Initialized
      type: boolean
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NoPlaneControlPlane is the Schema for the noplanecontrolplanes
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NoPlaneControlPlane
            properties:
              controlPlaneEndpoint:
                description: |-
                  controlPlaneEndpoint is the endpoint of the provisioned control plane.
                  Populated by the controller once ready. May be set by the user for
                  brownfield adoption.
                minProperties: 1
                properties:
                  host:
                    description: host is the hostname on which the API server is serving.
                    maxLength: 512
                    minLength: 1
                    type: string
                  port:
                    description: port is the port on which the API server is serving.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              credentialsSecretRef:
                description: |-
                  credentialsSecretRef references the Secret containing the noplane.io API key.
                  The Secret must contain a key named "apiKey".
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              planeID:
                description: |-
                  planeID is the noplane.io plane ID.
                  When set by the user, the controller skips creation and adopts the
                  existing control plane. Used for brownfield adoption.
                type: string
              replicas:
                default: 1
                description: replicas is the desired number of control plane replicas.
                format: int32
                type: integer
              version:
                description: version is the desired Kubernetes version (e.g. "v1.29.2").
                maxLength: 256
                minLength: 1
                type: string
            required:
            - credentialsSecretRef
            - version
            type: object
          status:
            description: status defines the observed state of NoPlaneControlPlane
            properties:
              conditions:
                description: conditions summarises the current state of the control
                  plane.
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              initialized:
                description: |-
                  initialized indicates the control plane has been successfully initialised
                  at least once. Once true, it is never set back to false.
                  CAPI core uses this to gate worker node bootstrap via CABPK.
                type: boolean
              planeID:
                description: |-
                  planeID is the noplane.io ID of the provisioned control plane.
                  Persisted here to survive controller restarts (crash recovery).
                type: string
              ready:
                description: ready indicates the control plane is operational.
                type: boolean
              readyReplicas:
                description: readyReplicas is the number of ready replicas.
                format: int32
                type: integer
              replicas:
                description: replicas is the total number of control plane replicas.
                format: int32
                type: integer
              selector:
                description: selector is the label selector string for the scale subresource.
                type: string
              version:
                description: version is the Kubernetes version currently running on
                  the control plane.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.replicas
      status: {}
